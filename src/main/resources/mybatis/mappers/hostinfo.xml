<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hostinfo">

	<select id="checkNo" parameterType="int" resultType="int">
		<![CDATA[
				select  hostNo
				from    host
				where   hostNo = #{hostNo}
		]]>
	</select>
	<select id="getHost" parameterType="int" resultType="com.javaex.vo.HostVo">
		<![CDATA[
				select hostNo,
       				   userNo,
				       zipcode,
				       adress1,
				       adress2,
				       adress3,
				       x,
				       y,
				       intro,
				       asking,
				       hostcost,
				       hostview
				from   host
				where  hostNo = #{hostNo}
		]]>
	</select>
</mapper>